# import cv2 as cv

# capture = cv.VideoCapture('TelloVideo/afternoon.mp4')
# skipped = None # only save every skipped (e.g. 80th) frame
# frame_num = 0
# file_name_num = 0
# while(True):
#     success, frame = capture.read()
#         if success:
#             if file_name_num%skipped == 0:
#                 _cv.({}.jpg'.format(file_name_num), frame) #save the image 
#         else:
#             break�/T += 1g�.release()